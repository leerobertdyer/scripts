# A simple command to move to this scripts directory
# Note: you must have the scriptsdir command installed and run chmodx 1st

cd $(scripts)

echo "You are now in your personal scripts directory:\n$(scripts)"
ls

